{
  "name": "litetrace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "release:artifacts": "node ./scripts/release-check.mjs",
    "release:check": "npm run typecheck && npm test && npm run build && npm run release:artifacts"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.26",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
